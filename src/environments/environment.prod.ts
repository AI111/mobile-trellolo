import {IAppConfig} from '../app/common/IAppConfig';

export const environment = {
  production: true,
  end: 'production'
};

export const config: IAppConfig = {
  tokenName: 'token',
  url: '/',
  port: '4200'
};
